# Conteúdo sensível: leads e contadores de rate limit.
# Nunca deve ser servido pela web.
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order deny,allow
  Deny from all
</IfModule>
